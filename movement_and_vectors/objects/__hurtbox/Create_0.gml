receive_damage = method(id, receive_damage);
receive_impulse_position = method(id, receive_impulse_position);
receive_impulse_direction = method(id, receive_impulse_direction);

if (owner != noone)
	alarm[11] = 1;